spring:
  application:
    name: greeting-kafka-streams
  profiles:
    active: local

---
spring:
  config:
    activate:
      on-profile: local
  kafka:
    streams:
      bootstrap-server: localhost:9092
      application-id: greeting-kafka-streams


